#!/bin/bash

# branch-lock - Securely protect or unprotect branches across multiple GitHub repositories
# Usage: ./branch-lock [lock|unlock]
# Dependencies: gh (GitHub CLI), yq
# Configuration: branch-lock.yaml
# Author: Germán Aliprandi - https://github.com/galiprandi
# License: MIT

set -euo pipefail

# Colors
Cyan='\033[0;36m'
Green='\033[0;32m'
Yellow='\033[1;33m'
Red='\033[0;31m'
NC='\033[0m'

# Config
CONFIG="branch-lock.yaml"

# Config check
check_config() {
  [[ -f "$CONFIG" ]] || {
    printf "${Red}[ERROR] Configuration file '$CONFIG' not found${NC}\n"
    
    # Create default config
    cat <<EOF > "$CONFIG"
# lock:
#   required_approvals: 6
# unlock:
#   required_approvals: 1
#
# repositories:
#   - org: galiprandi
#     repo: bash
#     branch: main
#     # Optional per-repository overrides (if omitted, global lock/unlock settings are used)
#     # lock:
#     #   required_approvals: 6
#     # unlock:
#     #   required_approvals: 1
EOF
    exit 1
  }
}

# Dependencies check
check_deps() {
  for cmd in gh yq; do
    if ! command -v "$cmd" &> /dev/null; then
      printf "${Red}[ERROR] %s is not installed. Install: https://cli.github.com | https://github.com/mikefarah/yq${NC}\n" "$cmd"
      exit 1
    fi
  done
}

# GitHub auth check
check_auth() {
  if ! gh auth status &> /dev/null; then
    printf "${Red}[ERROR] Not authenticated with GitHub CLI. Run 'gh auth login'${NC}\n"
    exit 1
  fi
}

# Load repos from config
load_repos() {
  check_config

  if ! yq eval '.repositories' "$CONFIG" &> /dev/null; then
    printf "${Red}[ERROR] Invalid YAML format or missing 'repositories' in '$CONFIG'${NC}\n"
    exit 1
  fi

  REPOS=()
  while IFS= read -r line; do
    REPOS+=("$line")
  done < <(yq eval '.repositories[] | "\(.org)/\(.repo) \(.branch)"' "$CONFIG" 2>/dev/null)
  [[ ${#REPOS[@]} -gt 0 ]] || {
    printf "${Red}[ERROR] No repositories defined in '$CONFIG'${NC}\n"
    exit 1
  }
}

# Confirm action
confirm() {
  local action=$1
  printf "${Cyan}Action:    %s${NC}\n" "$action"
  printf "${Cyan}Targets:   %d repositories${NC}\n" "${#REPOS[@]}"
  printf "\n"
  read -rp "Proceed? (yes/N): " CONFIRM
  [[ "$CONFIRM" == "yes" ]] || {
    printf "${Red}[ABORT] Operation cancelled${NC}\n"
    exit 0
  }
}

# Apply protection
protect() {
  local full="$1" branch="$2"
  printf "🔒 Protecting %s@%s... " "$full" "$branch"
  local out
  local payload
  # Derive org/repo for per-entry lookup
  local org repo
  org=${full%%/*}
  repo=${full##*/}
  
  # Determine required approvals from YAML (prefer per-repo lock override, fallback to global)
  local required_approving_review_count

  # Read lock settings: prefer per-repo, fallback to global .lock.required_approvals (default 6)
  local qrepo=".repositories[] | select(.org==\"$org\" and .repo==\"$repo\" and .branch==\"$branch\")"
  local repo_lock_approvals=$(yq eval "$qrepo.lock.required_approvals" "$CONFIG")
  if [[ "$repo_lock_approvals" != "null" ]]; then
    required_approving_review_count="$repo_lock_approvals"
  else
    required_approving_review_count=$(yq eval ".lock.required_approvals // 6" "$CONFIG")
  fi
  # Cap to GitHub limits
  if [[ "$required_approving_review_count" -gt 6 ]]; then
    required_approving_review_count=6
  fi
  if [[ "$required_approving_review_count" -lt 0 ]]; then
    required_approving_review_count=0
  fi
  # Build JSON payload: ONLY require N approvals; disable/omit other rules
  payload=$(cat <<JSON
{
  "required_pull_request_reviews": {
    "required_approving_review_count": $required_approving_review_count
  },
  "required_status_checks": null,
  "enforce_admins": false,
  "restrictions": null
}
JSON
)
  if out=$(gh api "repos/$full/branches/$branch/protection" \
    --method PUT \
    -H "Accept: application/vnd.github+json" \
    --input - 2>&1 <<< "$payload"); then
    printf "${Red}✔ PROTECTED${NC}\n"
    return 0
  else
    printf "${Red}✘ FAILED${NC}\n"
    printf "${Yellow}%s${NC}\n" "$out"
    return 1
  fi
}

# Remove protection
unprotect() {
  local full="$1" branch="$2"
  printf "🔓 Unprotecting %s@%s... " "$full" "$branch"
  # Change the number of required approvals on unlock
  local org repo
  org=${full%%/*}
  repo=${full##*/}
  local qrepo=".repositories[] | select(.org==\"$org\" and .repo==\"$repo\" and .branch==\"$branch\")"
  local repo_unlock_approvals=$(yq eval "$qrepo.unlock.required_approvals" "$CONFIG")
  local unlock_approvals
  if [[ "$repo_unlock_approvals" != "null" ]]; then
    unlock_approvals="$repo_unlock_approvals"
  else
    unlock_approvals=$(yq eval ".unlock.required_approvals // 0" "$CONFIG")
  fi
  # Cap approvals to GitHub limit
  if [[ "$unlock_approvals" -gt 6 ]]; then unlock_approvals=6; fi
  if [[ "$unlock_approvals" -lt 0 ]]; then unlock_approvals=0; fi

  local payload
  payload=$(cat <<JSON
{
  "required_pull_request_reviews": {
    "required_approving_review_count": $unlock_approvals
  },
  "required_status_checks": null,
  "enforce_admins": false,
  "restrictions": null
}
JSON
)
  if gh api "repos/$full/branches/$branch/protection" --method PUT -H "Accept: application/vnd.github+json" --input - <<< "$payload" >/dev/null 2>&1; then
    printf "${Green}🔓 UNPROTECTED${NC}\n"
    return 0
  else
    printf "${Red}✘ FAILED applying unlock approvals${NC}\n"
    gh api "repos/$full/branches/$branch/protection" --method PUT -H "Accept: application/vnd.github+json" --input - <<< "$payload" 2>&1 | sed 's/^/  /'
    return 1
  fi
}

# Main
main() {
  printf "${Cyan}🔐 branch-lock v1.0 — Multi-Repo Branch Protection Manager${NC}\n"
  printf "\n"

  check_deps
  check_auth
  load_repos

  case "${1:-}" in
    lock|unlock)
      ACTION="$1"
      ;;
    *)
      printf "Usage: %s [lock|unlock]\n" "$(basename "$0")"
      printf "  lock   %s\n" "Require N PR approvals (minimal protection)"
      printf "  unlock %s\n" "Set approvals on unlock (PUT minimal protection)"
      exit 1
      ;;
  esac

  confirm "$ACTION"

  local success=0 error=0
  for entry in "${REPOS[@]}"; do
    local full_repo=$(awk '{print $1}' <<< "$entry")
    local target_branch=$(awk '{print $2}' <<< "$entry")

    if [[ -z "$full_repo" || -z "$target_branch" ]]; then
      printf "${Red}[SKIP] Invalid entry: %s${NC}\n" "$entry"
      ((error++))
      continue
    fi

    if [[ "$ACTION" == "lock" ]]; then
      if protect "$full_repo" "$target_branch"; then
        ((success++))
      else
        ((error++))
      fi
    else
      if unprotect "$full_repo" "$target_branch"; then
        ((success++))
      else
        ((error++))
      fi
    fi
  done

  printf "\n"
  printf "${Green}[OK] Completed: %d success, %d error(s)${NC}\n" "$success" "$error"
  [[ $error -gt 0 ]] && printf "${Yellow}[NOTE] Check permissions or repository/branch names${NC}\n"
}

main "$@"